const INICIAL = 10

class ProfundizacionIt{
    constructor(map,pos,cajas) {
        this.map = map
        this.pos = [pos[0],pos[2]]
        this.cajas = cajas
    }

    neighbors(){
        var largo = this.map.length
        var ancho = this.map[0].length


    }

    solve(){
        let answer = this.pos

        return answer
    }
}
module.exports= ProfundizacionIt;